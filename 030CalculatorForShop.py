'''
โดยจากโจทย์ให้โปรแกรมทำการรับค่า
บรรทัดที่ 1 รับตัวเลขจำนวนเต็ม เป็นจำนวนของราคาสินค้าทั้งหมดที่กรอกเข้ามาในระบบ
บรรทัดที่ 2 - บรรทัดที่ n ให้ทำการรับราคาของสินค้าแต่ละชิ้นเข้ามาในระบบ โดยจะรับไปเรื่อย ๆ ตั้งแต่ตัวแรกจนถึงตัวสุดท้ายตามที่เรากรอกไว้ในบรรทัดแรก
'''
round_loop = int(input('How many pieces? >> '))
sum = 0
for i in range(round_loop):
  get = int(input('Value ? '))
  sum = sum + get
print(sum, 'THB')